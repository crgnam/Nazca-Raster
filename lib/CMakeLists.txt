# find_package(TBB REQUIRED PATHS ${CMAKE_SOURCE_DIR}/external/oneTBB/prefix/lib/cmake)

# add_library(
#     quipu

#     geometry.cpp
# )

# target_link_libraries(quipu PRIVATE PUBLIC tinyobjloader TBB::tbb lodepng)

# set_target_properties(quipu PROPERTIES LINKER_LANGUAGE CXX)